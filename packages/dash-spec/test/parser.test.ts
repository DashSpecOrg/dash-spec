describe('parseDashboard', () => {
  test.todo('parses a dashboard YAML document into a normalized object');
  test.todo('parses each card expr with pql-parser');
  test.todo('reports friendly validation errors for invalid layouts');
});
